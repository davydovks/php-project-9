{% extends "layout.twig" %}

{% set headerParams = {'activePage': 'urls'} %}

{% block content %}
<div class="container-lg mt-3">
    <div class="row">
        <h1>Сайты</h1>
        <div class="table-responsive">
            <table class="table table-bordered table-hover text-nowrap" data-test="urls">
                <tbody>
                    <tr>
                        <th>ID</th>
                        <th>Имя</th>
                        <th>Последняя проверка</th>
                        <th>Код ответа</th>
                    </tr>
                    {% for url in urls %}
                    <tr>
                        <td>{{ url.id }}</td>
                        <td>
                            <a href="{{ url_for('urls.show', {'id' : url.id}) }}">
                                {{ url.name }}
                            </a>
                        </td>
                        <td>{{ checks[url.id].createdAt }}</td>
                        <td>{{ checks[url.id].statusCode }}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
